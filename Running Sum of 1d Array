class Solution:
    def runningSum(self, nums: List[int]) -> List[int]:
        runningSum=0
        ans=[]
        for i in range(0,len(nums)):

            runningSum+=nums[i]
            ans.append(runningSum)
        return ans
        

from collections import Counter
class Solution:
    def majorityElement(self, nums: List[int]) -> int:
        d = Counter(nums)
        n = len(nums)
        for key,value in d.items():
            if(value>n//2):
                return key
        
        
